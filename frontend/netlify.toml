[build]
  command = "apt-get update && apt-get install -y postgresql-client libpq-dev && pip install -r requirements.txt && npm run build"
  publish = "frontend/build"

[build.environment]
  PYTHON_VERSION = "3.14"